import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.darkfactory.homeclaw',
  appName: 'HomeClawRemote',
  webDir: 'dist'
};

export default config;
